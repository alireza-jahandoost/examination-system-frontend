test("if user is authenticated, user can register in the exam", () => {});

test("if user is not authenticated, user will see the login page", () => {});

test("if user is not authenticated, user can login and then register in exam", () => {});

test("if user is not authenticated, user can register and then register in exam", () => {});
